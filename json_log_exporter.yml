---
labels:
  foo: bar
log_groups:
  - name: orders
    files:
      - ./test.log
    labels:
      fizz: buzz
    metrics:
      - name: count_total
        type: counter
        export_to: export1
        labels:
          user_id: "{{.user_id}}"
      - name: count_total
        type: counter
        export_to: export2
        labels:
          status: "{{.status}}"
exports:
  - name: export1
    path: /metrics/1
  - name: export2
    path: /metrics/2